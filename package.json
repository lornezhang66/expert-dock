{
  "name": "expert-dock",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply expert-dock --local",
    "db:remote": "wrangler d1 migrations apply expert-dock --remote",
    "test": "node --test"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "wrangler": "^4.104.0"
  }
}
